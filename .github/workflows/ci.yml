name: CI

on:
  push:
  pull_request:

jobs:
  e2d_ci:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        image:
          - archlinux:latest
          - debian:12
          - debian:10
          - ubuntu:24.04
          - ubuntu:22.04
          - ubuntu:20.04
        mode:
          - root
          - eva
          - adam
        exclude:
          - image: archlinux:latest
            mode: root # Arch image does not support root mode in this setup
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          docker build \
            -f ci/Dockerfile \
            --build-arg BASE_IMAGE=${{ matrix.image }} \
            --build-arg MODE=${{ matrix.mode }} \
            -t ci:test .
      - name: Run
        run: |
          docker run --rm \
            --user ${{ matrix.mode }} \
            ci:test
